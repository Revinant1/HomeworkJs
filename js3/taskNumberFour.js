"use strict";

function nextNumber(primeNumber) {
    for (let d = 2; d <= 100; d++) {
        for (let b = 2; b < d; b++) {
            if (primeNumber = d % b === 0);
            console.log(primeNumber);
        }

    }
}
function nextNumber(d){
    
}